#include <iostream>
using namespace std;
int main()
{
    int r;
    float area;
    cout << " Enter the radius of a circle :";
    cin >> r;
    area = (r * r) * (3.15);
    cout << "Area of the circle is : " << area;

    return 0;
}